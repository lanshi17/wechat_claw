{
  "name": "wechat-claw",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "start:mvp": "npm run build && node dist/cli.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
